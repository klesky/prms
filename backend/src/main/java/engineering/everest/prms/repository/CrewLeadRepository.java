package engineering.everest.prms.repository;

import engineering.everest.prms.entity.CrewLead;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CrewLeadRepository extends JpaRepository<CrewLead, String> {
}
